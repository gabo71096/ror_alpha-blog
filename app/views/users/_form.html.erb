<%= render 'shared/errors', obj: @user %>
<%= form_with(model: @user, local: true, class: 'shadow p-3 mb-5 bg-white rounded') do |f| %>
  <div class="row mb-3 text-start">
    <%= f.label :username, class: 'col-form-label col-sm-2' %>
    <div class="col-sm-10">
      <%= f.text_field :username, class: 'form-control shadow rounded', placeholder: 'Enter a username' %>
    </div>
  </div>
  <div class="row mb-3 text-start">
    <%= f.label :email, class: 'col-form-label col-sm-2' %>
    <div class="col-sm-10">
      <%= f.email_field :email, class: 'form-control shadow rounded', placeholder: 'Enter an email' %>
    </div>
  </div>
  <div class="row mb-3 text-start">
    <%= f.label :password, class: 'col-form-label col-sm-2' %>
    <div class="col-sm-10">
      <%= f.password_field :password, class: 'form-control shadow rounded', placeholder: 'Enter a password' %>
    </div>
  </div>
  <%= f.submit 'Sign Up', class: 'btn btn-primary mb-3' %>
<% end %>
<%= link_to 'Return to articles', articles_path, class: 'btn btn-outline-success' %>
